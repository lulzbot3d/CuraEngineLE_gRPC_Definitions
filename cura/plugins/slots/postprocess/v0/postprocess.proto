syntax = "proto3";

package cura.plugins.slots.postprocess.v0;

service PostprocessModifyService {
  rpc Call(CallRequest) returns (CallResponse) {}
}

message CallRequest {
  string gcode_word = 1;
}

message CallResponse {
  string gcode_word = 1;
}
